specVersion: "v1beta"
serviceId: "projeck"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "projeck-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
